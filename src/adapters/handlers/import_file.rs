//! Legacy import_file module — functionality merged into import.rs.
//!
//! This module is intentionally empty. All CSV import functionality is now in
//! the `import` module with per-entity handlers.
